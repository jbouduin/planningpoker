{
  "ignore": ["**/*.test.ts", "**/*.spec.ts", ".git", "node_modules"],
  "watch": ["src", "../shared-lib/src"],
  "exec": "npm run build:prod && node dist/prod/server.js",
  "ext": "ts",
  "env": {
    "NODE_ENV": "development",
    "DEBUG": "socket.io*"
  }
}
