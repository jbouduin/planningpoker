export type ScrumMasterButtonsComponentState = {
  disableChangeCardSet: boolean;
  disableForceReveal: boolean;
  disableReveal: boolean;
  showClear: boolean;
  showReveal: boolean;
  showStart: boolean;
};
