export type ParsedCommandOptions = {
  purge: boolean;
  enums: Array<string>;
  enumOutput: string;
  ignore: Array<string>;
  replace: boolean;
  verbose: boolean;
  config: string;
  output: string;
};
